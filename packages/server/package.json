{
  "name": "@cll/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc && cp src/db/schema.sql dist/db/schema.sql",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cll/shared": "workspace:*",
    "express": "^5.0.0",
    "better-sqlite3": "^11.0.0",
    "yaml": "^2.5.0",
    "cors": "^2.8.5",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/cors": "^2.8.0",
    "@types/node": "^20.0.0",
    "typescript": "*",
    "tsx": "^4.0.0",
    "vitest": "^2.0.0"
  }
}
